# Migration from bridge AE skills

All nine original skill names remain available. Ten construction references retain the detailed rig knowledge, with media defaults generalized. Other active entries are curated local adaptations covering orchestration, animation, layout, depth, glass and transitions; the full original text remains in the archive for follow-up adaptation. This is not a claim that every bridge capability was ported.

The runtime now discovers typed native operations through `ae_catalog` and executes them through `ae_do`. Removed executable assumptions include cloud authentication, remote host routing, HTML-to-scene and Lottie converters, cloud generation, a blanket 3D prohibition, personal model defaults and automatic import compatibility.

Skills use progressive disclosure: one entry or one reference per request. Unknown names fail explicitly. The runtime verifies pinned document hashes and never exposes the archive. Original files and hashes are retained for review; application-specific recipes still need live visual validation.
