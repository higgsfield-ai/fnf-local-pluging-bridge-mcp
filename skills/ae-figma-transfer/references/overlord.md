# Overlord Transfer

Use this route only when the user requests the installed Overlord workflow. Inspect the available plugin interface and current support for the selected source before starting. A transfer request does not authorize installation or license activation.

Confirm the selected Figma frame and destination in the already open After Effects project. Transfer through the plugin's supported operations, then inspect actual native layers, media paths and source-to-destination identities. Apply the main skill's project, hierarchy, timing and verification rules, reading source or geometry guidance when the operation requires it.

Do not assume plugin success guarantees editable text, faithful effects or original media. Report unsupported features and missing controls precisely. If the plugin cannot preserve the required project state or expose enough information to verify the result, report that limitation before dependent work.
